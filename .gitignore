/.venv/
/datasets/
/runs/*/pred/
/runs/*/overlay/*.npy
__pycache__/
*.pyc
*.log
.venv/
__pycache__/
*.pyc
*.pid
*.log

# Big, generated, or local-only
datasets/
sim/logs/
training/runs/

# Track dataset tooling and smoke manifest
!scripts/datasets/
!training/tests/data/
!datasets/manifest.json

# Unignore dataset tests
!training/
!training/tests/
!training/tests/data/
!training/tests/data/test_dataset_diff_smoke.py

# Backups & local outputs
.pre-commit-config.yaml.bak*
artifacts/domain_randomization/
artifacts/onnx/*.bak-*

# maps & costmaps (rasters)
maps/build/
maps/costmaps/

# Map artifacts
maps/build/
maps/costmaps/
tiles/
# --- maps/tiles & heavy rasters (generated) ---
tiles/
maps/build/
maps/costmaps/
# sidecar stats files we don't want
*.tif.aux.xml
# MBTiles and other publish artifacts (generated)
*.mbtiles

# maps/tiles artifacts
tiles/
maps/build/
maps/costmaps/
*.tif.aux.xml

# === repo hygiene: keep heavy map data out of git ===
artifacts/
maps/build/
maps/costmaps/
maps/masks/
maps/src/
cgi-bin/
*.bak

# Perf traces & local bundles
onnxruntime_profile__*.json
artifacts/maps/*.zip

# Public datasets (keep heavy data out of Git)
datasets/public/*/raw/**
datasets/public/*/processed/**
!datasets/public/*/README.md
!training/scripts/data/**
