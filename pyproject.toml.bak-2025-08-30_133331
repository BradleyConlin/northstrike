[tool.black]
line-length = 100
target-version = ["py310"]

[tool.isort]
profile = "black"
line_length = 100
src_paths = ["training", "sim"]

[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = [
  "training/runs",
  "datasets",
  ".venv*",
  "sim/logs",
  "sim/release",
  "training/reports",
  "sim/scripts/_archive",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E203", "W503"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["training/tests", "sim/tests"]
python_files = ["test_*.py", "*_test.py"]
